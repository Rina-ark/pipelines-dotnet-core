# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'

# test
# Delete files(ファイルの削除)
- task: DeleteFiles@1
  inputs:
    SourceFolder: '/test/pass/'
    Contents: 'myFileShare'

# Command line(コマンドライン)
- task: CmdLine@2
  inputs:
    script: 'echo test'

# Batch script(バッチスクリプト)
- task: BatchScript@1
  inputs:
    filename: '/test/pass/'

# MSBuild
- task: MSBuild@1
  inputs:
    solution: 'test'

# Visual Studio build(Visual Studioのビルド)
- task: VSBuild@1
  inputs:
    solution: '/test/pass/'

# Index sources and publish symbols(ソースのインデックスおよびシンボルの発行)
- task: PublishSymbols@2
  inputs:
    SearchPattern: '**/bin/**/*.pdb'
    SymbolServerType: ''

# Copy files(ファイルのコピー)
- task: CopyFiles@2
  inputs:
    Contents: '/test/pass/'
    TargetFolder: '$(build.artifactstagingdirectory)'